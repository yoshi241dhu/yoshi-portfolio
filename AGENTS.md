# AGENTS.md — このリポジトリで作業するとき

> このファイルは、**AI（Claude Code / OpenCode など）に作業を頼むときの前提**をまとめたものです。
> 人が読んでも構いません。手順そのものは `README.md` にあります。

## このリポジトリは何か

作品を並べて **GitHub Pages** で公開するための、最小のポートフォリオテンプレートです。

**使う人はエンジニアではありません。** ここが最大の制約です。プログラミング未経験者が対象です。

## 絶対に守ること

### 1. ビルドを増やさない

`index.html` をそのまま配信します。npm も、フレームワークも、トランスパイルも入れません。

理由は、**失敗したときに本人が直せなくなる**からです。
ビルドが1つ入るだけで「なぜか公開されない」状態が生まれ、そこで手が止まります。
見た目を良くするより、**止まらないこと**を優先してください。

### 2. ファイルを増やさない

構成はこれだけです。**新しいファイルを提案しないでください。**

```
index.html    本文。ここだけ書き換えれば完成する
style.css     見た目。触らなくてよい
images/       作品画像を入れる
README.md     手順（受講者向け）
```

`package.json` / `src/` / `components/` などを作らないこと。
「整理のため」に分割すると、**どこを直せばいいか分からなくなります。**

### 3. HTML は「コピーして書き換える」形を保つ

作品を増やす操作は、`<figure class="work">` ブロックをコピーして貼るだけにしてあります。
ループやテンプレート構文を導入しないでください。**コピペで増やせることが仕様です。**

### 4. コメントで場所を示す

`index.html` の書き換え箇所は `<!-- ▼ ここから書き換えます -->` で囲んであります。
編集を提案するときは、**この目印を消さないでください。**

## AI に頼むときの言い方（受講者向け）

うまくいく頼み方の例です。

```
index.html の作品を1つ増やしてください。
タイトルは「夜の図書館」、第3回の作品です。
画像は images/work-03.png です。
```

```
自己紹介の文章を、映像を専攻している学生向けに書き直してください。
3行以内でお願いします。
```

**避けたい頼み方**（構成が壊れます）

```
❌ このサイトをReactで作り直して
❌ もっとかっこよくして（何をどうするか決まっていない）
❌ ビルドツールを入れて最適化して
```

## CI/CD

`.github/workflows/pages.yml` が `main` への保存を検知して公開します。

- **ビルドしません。** ファイルをそのまま配ります
- 公開前に2つだけ確認します
  - `index.html` があるか（無ければ失敗）
  - テンプレートの文言が残っていないか／画像があるか（警告のみ、公開は止めない）

**警告で止めないのは意図的です。** 締切直前に「警告が出て公開できない」状態を作らないためです。

手動で再実行したいときは、GitHub の Actions タブから `pages` → `Run workflow`。

## 作品の権利について

このテンプレートは生成AIで作った作品を並べる前提です。`index.html` のフッターに
**AI使用の明示**が入れてあります。**消さないでください。**

各作品にプロンプトを載せる `<details>` も同じ理由です。
再現性の記録であり、制作の再現性を残すためです。

## 提出

公開されるのは次のURLです。**リポジトリのURLとは別**なので注意してください。

```
https://<あなたのユーザー名>.github.io/<リポジトリ名>/
```

Settings → Pages で有効化すると発行されます。
